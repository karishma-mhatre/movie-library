import { Movie } from "./movie";

export class MovieCollection {
    constructor(movies) {
        this.movies = movies;
    }

    addMovie = (movie) => {
        this.movie.push(movie);
    }

    removeMovie = (movie) => {

    }
}